{
  "name": "taskping-root",
  "version": "1.0.0",
  "description": "TASKping Root Workspace",
  "scripts": {
    "install-all": "npm install && npm run install-deps",
    "install-deps": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "build": "npm run install-deps && npm run build --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start-backend": "npm run start --prefix backend",
    "start-frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
